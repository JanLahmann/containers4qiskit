# Generated by .github/scripts/build-pages-data.py at Pages deploy time.
# Keep out of VCS so the source of truth stays the versions/ directory.
docs/versions.json

# Generated by .github/scripts/build-badges.py at Pages deploy time.
# Same reasoning — regenerated from versions.json on every deploy.
docs/badges/

# Generated by .github/scripts/build-admin-stats.py at Pages deploy time.
# Pulled live from GHCR API + mybinder archive; never committed.
docs/admin/stats.json

# Python bytecode cache (created by the scaffold + stats scripts if
# imported via importlib during local smoke tests).
__pycache__/
*.pyc

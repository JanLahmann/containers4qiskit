# Pinned reproducible environment for Qiskit 1.4 (XL flavor).
#
# 1.x is a different API generation from the docs-tester pin set
# (which targets 2.3+). The qiskit-addon-* packages, qiskit-serverless,
# qiskit-ibm-catalog, qiskit-ibm-transpiler, and the docs-tester pinned
# versions of qiskit-aer/-ibm-runtime/-experiments all require qiskit
# >=2.x and are dropped here. What's left: a qiskit 1.4 core, the older
# resolved versions of aer/runtime/experiments, plus the shared
# scientific stack from ../_xl-base.txt — and matplotlib, which the
# 2.x flavors don't bundle but 1.x notebooks more commonly assume.

-r ../_xl-base.txt

qiskit~=1.4.0
qiskit-aer
qiskit-ibm-runtime
qiskit-experiments
matplotlib
